pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }

    plugins {
        id("io.github.leanish.java-conventions") version "0.5.4"
    }
}

rootProject.name = "sqs-codec"
